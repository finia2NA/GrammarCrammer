# Host and port of the Express API server.
# Defaults to localhost:3001 if not set.
# Set DEV_SERVER_HOST to your machine's IP when testing on a physical device.
DEV_SERVER_HOST=localhost
DEV_SERVER_PORT=3001
